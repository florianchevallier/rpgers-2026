name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    name: Validate Code
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      # --ignore-scripts saute lefthook ; prisma generate est dans `validate`
      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run validation
        run: npm run validate
